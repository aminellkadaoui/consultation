# تشغيل صفحة الاستشارة ولوحة الإدارة

الصفحة العامة في `index.html`، ولوحة الإدارة في `admin.html`. أُضيف ربط Firebase وقواعد الوصول؛ تفعيل القواعد وحساب الإدارة في مشروعك هو الخطوة اللازمة لاستقبال الطلبات وحفظ تعديلات الصفحة.

## 1. جهّز Firebase

1. افتح [مشروع elite-editor-9fe62 في Firebase](https://console.firebase.google.com/project/elite-editor-9fe62/overview).
2. من **Authentication → Sign-in method** فعّل **Email/Password** إن لم يكن مفعّلًا.
3. من **Authentication → Users** أنشئ حسابك للإدارة، أو استخدم حسابك الموجود، ثم انسخ **UID** الخاص به. لا تضع كلمة المرور داخل ملفات الموقع.
4. افتح **Firestore Database**. الكود يستعمل قاعدة `(default)` افتراضيًا. إذا كانت قاعدة مشروعك اسمها `default` بدون أقواس، أو لها اسم آخر، ضع الاسم الصحيح في `firebase.databaseId` داخل `consultation-config.js`.
5. أضف نطاق الموقع إلى **Authentication → Settings → Authorized domains**. في GitHub Pages يكون النطاق `aminellkadaoui.github.io`؛ لا تضف مسار `/consultation/` ضمن النطاق.

الموقع يستعمل إعدادات Firebase العامة الموجودة أصلًا في المشروع. لا يحتاج مفتاح Service Account ولا يستعمل مجموعة العملاء القديمة `client_context`.

## 2. أضف قواعد الوصول

افتح `firestore.rules` واستبدل `REPLACE_WITH_YOUR_FIREBASE_AUTH_UID` بمعرّف حسابك من الخطوة السابقة. يمكن وضع أكثر من UID داخل القائمة عند الحاجة.

**المشروع مشترك مع الموقع القديم: لا تستبدل قواعده كلها بالملف الجديد.** احتفظ بنسخة من القواعد الحالية، ثم انسخ الجزء بين `BEGIN CONSULTATION RULES` و`END CONSULTATION RULES` إلى داخل:

```text
match /databases/{database}/documents {
  // قواعد مشروعك الحالية، ثم قواعد الاستشارة هنا
}
```

احتفظ بغلاف `rules_version` و`service cloud.firestore` مرة واحدة فقط. إذا كانت القاعدة جديدة وخالية تمامًا، يمكن استعمال ملف `firestore.rules` كاملًا بعد تعديل UID.

راجع أي قاعدة عامة مثل `match /{document=**}` تسمح بالقراءة أو الكتابة للجميع أو لكل حساب مسجّل. قواعد Firestore تتجمع بالسماح: أي قاعدة أوسع تسمح بالوصول قد تتجاوز القيود الجديدة. احصر قواعد التطبيقات القديمة بمجموعاتها بدل ترك سماح عام، ثم انشر القواعد للقاعدة الصحيحة.

ما تسمح به قواعد الاستشارة:

| البيانات | الزائر | حساب الإدارة المحدد بالـ UID |
| --- | --- | --- |
| `consultation_requests` | إرسال طلب مكتمل فقط بحالة `new`؛ لا يستطيع قراءة الطلبات أو تعديلها | قراءة الطلبات وتعديل الحالة والملاحظات الخاصة |
| `consultation_settings/public` | قراءة إعدادات الصفحة فقط | حفظ العنوان، الفيديو، الآراء وبقية إعدادات الصفحة |

لا توضع بيانات المتقدمين أو ملاحظاتك داخل إعدادات الصفحة، لأنها متاحة للقراءة العامة. الأرشفة تحفظ الطلب؛ الحذف النهائي غير متاح من اللوحة.

## 3. اضبط الصفحة من لوحة الإدارة

1. افتح `admin.html` من نفس عنوان موقعك، وسجّل الدخول بحساب Firebase الذي أضفت UID الخاص به.
2. أضف رابط فيديو الاستشارة وصورته إن توفرت. استعمل رابط HTTPS للفيديو المستضاف؛ رفع ملفات الفيديو نفسه يحتاج خدمة استضافة فيديو.
3. أضف الآراء الحقيقية: الاسم، وصف قصير اختياري، ونص أو رابط فيديو أو رابط صورة. لا يلزم نص للرأي إذا أضفت فيديو أو صورة. الحد الأقصى 12 رأيًا.
4. راجع العنوان والسعر ومدة الجلسة ومدة إرسال خطة العمل. القيم الافتراضية: **300 درهم مغربي، 60 دقيقة، وخطة خلال 24 ساعة**.
5. احفظ التعديلات، ثم افتح الصفحة العامة للتأكد من ظهورها.

الإعدادات المحفوظة في Firebase تتقدم على القيم الافتراضية في `consultation-config.js`. إذا لم تُحفظ إعدادات بعد، تعمل الصفحة بالقيم الموجودة في الملف. ظهور الصفحة وحده لا يعني أن استقبال الطلبات أصبح مفعّلًا؛ جرّب الإرسال بعد نشر قواعد Firebase.

## 4. جرّب مسار الطلب

1. افتح الصفحة كزائر واملأ المراحل الخمس ببيانات تجريبية واضحة.
2. أرسل الطلب وتأكد من ظهور تأكيد الإرسال ورقم الطلب. لا يُعرض نجاح إذا رفضت قاعدة البيانات الطلب.
3. افتح لوحة الإدارة، وحدّث قائمة الطلبات. راجع الاسم وبقية الإجابات والحالة `new`.
4. جرّب تغيير الحالة وإضافة ملاحظة خاصة، ثم أعد تحميل اللوحة للتأكد من الحفظ.
5. جرّب الآراء والفيديو على الهاتف والكمبيوتر.

ومن **Rules Playground** أو محاكي Firebase، تحقق من الحالات الآتية قبل الاعتماد على الاستقبال:

| الاختبار | النتيجة المطلوبة |
| --- | --- |
| زائر يقرأ طلبًا أو قائمة الطلبات | مرفوض |
| زائر يرسل طلبًا ناقصًا، أو يضيف `privateNotes`، أو يختار حالة غير `new` | مرفوض |
| زائر يحاول تعديل طلب أو إعدادات الصفحة | مرفوض |
| حساب مسجّل لم يُضف UID الخاص به يحاول قراءة الطلبات | مرفوض |
| حساب الإدارة يقرأ الطلبات ويغير الحالة أو الملاحظات | مسموح |
| زائر يقرأ `consultation_settings/public` | مسموح |

طلب الإرسال الصحيح يحتوي الحقول الاثني عشر، `status: 'new'`، و`schemaVersion: 1`، و`submittedAt` بتوقيت الخادم. الكود يضيف هذه القيم تلقائيًا. عند اختبار الإنشاء بالقواعد يلزم محاكاة `serverTimestamp`/وقت الطلب؛ إدخال تاريخ عشوائي يُرفض.

## 5. انشر الموقع على GitHub Pages

بعد رفع الملفات إلى مستودع `consultation`:

1. افتح **Settings → Pages** داخل المستودع.
2. اختر **Deploy from a branch** ثم فرع `main` ومجلد `/(root)`، واضغط **Save**.
3. انتظر اكتمال النشر. رابط الصفحة المتوقع عند تفعيل GitHub Pages هو `https://aminellkadaoui.github.io/consultation/`، ولوحة الإدارة في `https://aminellkadaoui.github.io/consultation/admin.html`.

يجب فتح الموقع عبر HTTPS أو خادم محلي أثناء التطوير؛ فتح ملفات HTML مباشرة من الجهاز لا يشغّل وحدات JavaScript بالطريقة المطلوبة. هذه الخطوات لا تنشر إعدادات Firebase تلقائيًا.

## إدارة الطلب بعد وصوله

تراجع الطلب، ثم تتواصل مع صاحبه إذا كانت الاستشارة مناسبة. بعد ذلك ترسل تفاصيل الحجز والدفع. الحالة داخل اللوحة تتدرج حسب حاجتك: **جديد ← قيد المراجعة ← مناسب ← تم الحجز ← مكتمل**، مع إمكانية الأرشفة.

إضافة رابط الحجز لا تفعّل تحصيل الدفع أو حجز الموعد تلقائيًا. لا توجد رسائل واتساب أو بريد تلقائية، ولا تُنشأ خطة العمل تلقائيًا؛ هذه خطوات تنفذها أنت بعد مراجعة الحالة والجلسة.

## إذا ظهر خطأ

| الرسالة أو المشكلة | ما تراجعه |
| --- | --- |
| `permission-denied` عند فتح الطلبات أو الحفظ | UID الصحيح، القواعد المنشورة، واسم قاعدة البيانات |
| `permission-denied` عند إرسال الفورم | نشر قواعد `consultation_requests` على نفس القاعدة التي في `consultation-config.js` |
| تعذر تسجيل الدخول | تفعيل Email/Password، وجود الحساب، والبريد وكلمة المرور |
| الفيديو أو الصورة لا يظهران | رابط HTTPS صالح ونوع فيديو تدعمه الصفحة؛ الرابط العام وليس رابط إدارة الفيديو |
| لا تظهر الطلبات القديمة من الموقع السابق | هذا مقصود: الاستشارة لها مجموعة مستقلة، ولا تنسخ بيانات `client_context` |

قواعد الوصول تتحقق من البيانات والصلاحيات، لكنها لا تضع حدًا لعدد الطلبات من كل زائر. إذا احتجت منع الإرسال الآلي على نطاق أكبر، يمكن إضافة App Check والتحقق من الإساءة بخدمة خلفية؛ ذلك لم يُفعّل ضمن هذه النسخة.

مراجع Firebase: [التحقق من حقول المستندات](https://firebase.google.com/docs/firestore/security/rules-fields)، [كيفية تداخل القواعد](https://firebase.google.com/docs/firestore/security/rules-structure)، [إدارة قواعد البيانات وأسمائها](https://firebase.google.com/docs/firestore/manage-databases).
