# إشعارات طلبات الاستشارة بالبريد

**الحالة: الكود جاهز محليًا، والإرسال معطّل افتراضيًا. رفع الملفات إلى GitHub لا يشغّل الإشعارات.** يلزم نشر الدالة في Firebase بعد ربط خدمة البريد والتأكد من إعدادات المشروع. لم يتم إرسال بريد تجريبي أو تفعيل الفوترة ضمن تجهيز هذا الكود.

عندما يُحفظ طلب جديد في `consultation_requests`، تعمل دالة من الجيل الثاني على قاعدة البيانات `default` في مشروع `elite-editor-9fe62`. ترسل اسم الشخص، وتاريخ الطلب، ومختصر المشكلة، ورابط الطلب داخل لوحة الإدارة. لا ترسل كل إجابات الفورم، ولا تتطلب إضافة بريد العميل إلى الفورم.

حفظ الطلب مستقل عن وصول الإيميل. تعطل خدمة البريد لا يفقد الطلب ولا يجعل الزائر يرسل الفورم مرة ثانية.

## ما يلزم قبل التفعيل

1. تحديد بريد صاحب الموقع الذي يستقبل التنبيه.
2. حساب Resend ومفتاح API للإرسال، وعنوان مرسل مسموح به لدى الخدمة. للإرسال الفعلي يلزم مرسل من نطاق تم التحقق منه، أو استخدام وضع الاختبار إلى البريد الذي تسمح به الخدمة. لا يوضع مفتاح API في الموقع أو في GitHub.
3. مشروع Firebase على خطة Blaze وصلاحية نشر Cloud Functions. تفعيل هذه الخطة وربط الفوترة خطوة منفصلة؛ لم ينفذها هذا الكود. [متطلبات Firebase](https://firebase.google.com/docs/functions/get-started).
4. مراجعة موقع قاعدة Firestore ثم اختيار منطقة مناسبة للدالة. اسم قاعدة البيانات هو `default` دون أقواس. قواعد البيانات المسماة تحتاج الجيل الثاني. [توثيق أحداث Firestore](https://firebase.google.com/docs/functions/firestore-events).

## النشر بعد توفير الإعدادات

يتطلب النشر Node.js 22 وFirebase CLI وتسجيل دخول إلى الحساب المخول بإدارة المشروع. [النسخ المدعومة](https://firebase.google.com/docs/functions/manage-functions).

من مجلد `consultation`:

```sh
npm --prefix functions install
npm --prefix functions test
firebase login
firebase functions:secrets:set RESEND_API_KEY --project elite-editor-9fe62
```

أدخل المفتاح في شاشة الإدخال السرية للأمر الأخير. لا ترسله في المحادثة، ولا تكتبه ضمن الأمر، ولا تحفظه في ملفات الواجهة. [إعداد الأسرار في Firebase](https://firebase.google.com/docs/functions/config-env).

انسخ `functions/.env.example` إلى `functions/.env.elite-editor-9fe62` محليًا، ثم أكمل:

```dotenv
CONSULTATION_EMAIL_ENABLED=true
CONSULTATION_NOTIFY_EMAIL=YOUR_CONFIRMED_RECIPIENT
CONSULTATION_EMAIL_FROM=YOUR_VERIFIED_SENDER
CONSULTATION_FUNCTION_REGION=YOUR_SELECTED_REGION
```

استبدل القيم بأخرى حقيقية. البريد المرسل هنا يكون عنوان بريد فقط، دون اسم عرض. الملف المحلي مستثنى من Git. بعد مراجعة الإعدادات:

```sh
firebase deploy --only functions:notifyConsultationRequest --project elite-editor-9fe62
```

هذا الأمر ينشر دالة الإشعار فقط. لا يغيّر قواعد Firestore أو موقع GitHub Pages. بعد النشر يُرسل طلب اختبار واضح، ثم يُتحقق من ظهوره في الإدارة ومن وصول رسالة واحدة. تبقى الرسالة بحالة «قبلتها خدمة البريد» حتى يؤكد سجل Resend التسليم؛ قبول الخدمة لا يعني بالضرورة وصولها إلى صندوق الوارد.

## منع التكرار والتعامل مع الفشل

تحتفظ الدالة بسجل خاص لكل طلب في `consultation_email_deliveries`. لا تمنح الزوار صلاحية القراءة أو الكتابة فيه. قواعد الموقع الحالية تمنع الوصول غير المصرح به افتراضيًا؛ تستخدم الدالة صلاحيات الخادم.

يُحفظ نص البريد الأول ومعرّف إرسال ثابت، فلا تغيّر المحاولات اللاحقة محتواه. هناك حجز مؤقت يمنع التنفيذ المتزامن، وحالة `accepted` تمنع إعادة الإرسال بعد قبول Resend للرسالة. يتم تكرار المحاولة عند تعطل الشبكة أو رفض الخدمة المؤقت. لا تُسجل مفاتيح البريد أو إجابات العملاء في سجلات التشغيل.

مفاتيح منع التكرار في Resend صالحة 24 ساعة. لذلك تتوقف المحاولات التلقائية غير المحسومة بعد 23 ساعة وتنتقل إلى `manual_review`؛ هذا يتجنب إعادة إرسال رسالة ربما وصلت بالفعل. أخطاء الإعداد الدائمة تنتقل إلى الحالة نفسها. افحص سجل Resend قبل أي إعادة إرسال يدوية، ولا تحذف سجل الإرسال أو تغيّر معرّفه لمجرد محاولة الإصلاح. [آلية Resend](https://resend.com/docs/dashboard/emails/idempotency-keys).

لإيقاف الإرسال، اجعل `CONSULTATION_EMAIL_ENABLED=false` وأعد نشر الدالة. الطلبات التي تصل أثناء التعطيل أو قبل أول نشر تبقى في الإدارة، ولا يتم إرسال تنبيهاتها بأثر رجعي تلقائيًا.

## التحقق المنجز

اختبارات محلية بدون إرسال فعلي تغطي نسختي بيانات الفورم، وتعقيم HTML، واختصار المعلومات، وصحة رابط الطلب، وإخفاء البيانات الخاصة، وثبات معرّف الإرسال، وحالات منع التكرار، وأخطاء خدمة البريد. يلزم اختبار حي بعد إعداد الحساب والنشر.
